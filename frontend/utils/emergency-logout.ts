
/**
 * This function has been disabled as part of removing the emergency logout feature.
 * The function remains to prevent breaking imports but does not perform any logout actions.
 */
export const performEmergencyLogout = async () => {
  console.log("Emergency logout has been disabled");
  
  // No longer performs emergency logout actions
  return true;
};
